#include<stdio.h>
 
int main()
{
 int a, b, c;
 
 printf("Enter two numbers to find difference\n");
 scanf("%d%d", &a, &b);
 
 c = a - b;
 
 printf("Difference of the numbers = %d\n", c);
 
 return 0;
}
